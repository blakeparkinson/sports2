<div class="answer-container">
        {{#compare league 'nba'}}
          <div class="starters">
            {{#each roster.starters}}
              <div class="answer-row {{render}}" data-id="{{player_id}}">
                <p class="answered-player">{{render_position ../league position}}</p>
              </div>
            {{/each}}
          </div>
          <div class="answer-container bench">
            {{#each roster.bench}}
              <div class="answer-row {{render}} empty">Bench {{inc @index}}</div>
            {{/each}}
          </div>
         {{/compare}}
         {{#compare league '!=' 'nba'}}
           {{#each roster}}
             <div class="player bench">
              <div class="answer-row {{render}}" data-id="{{player_id}}">
                {{#compare ../league '!=' 'goats'}}
                  <p class="answered-player">{{render_position ../league position}}</p>
                {{/compare}}
                {{#compare ../league 'goats'}}
                  <p class="answered-player">{{rank}}</p>
                {{/compare}}  
              </div>
             </div>
           {{/each}}
         {{/compare}}
      </div>